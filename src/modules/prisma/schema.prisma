//******** AUTO GENERATED FILE, DO NOT EDIT.  *********

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["fullTextSearch"]
  binaryTargets   = ["native", "rhel-openssl-1.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model MatchEntity {
  id          Int           @id @default(autoincrement())
  uuid        String        @unique @default(uuid()) @db.Uuid
  createdAt   DateTime      @default(now())
  updatedAt   DateTime      @default(now()) @updatedAt
  round       Int
  score1      Int?
  score2      Int?

  player1     PlayerEntity  @relation("player_matches1", fields: [player1Uuid], references: [uuid])
  player1Uuid String        @db.Uuid
  player2     PlayerEntity  @relation("player_matches2", fields: [player2Uuid], references: [uuid])
  player2Uuid String        @db.Uuid

  @@map("match")
}

model PlayerEntity {
  id        Int      @id @default(autoincrement())
  uuid      String   @unique @default(uuid()) @db.Uuid
  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt
  name      String
  points    Int @default(0)
  played    Int @default(0)
  won       Int @default(0)
  lost      Int @default(0)
  drawed    Int @default(0)
  for       Int @default(0)
  against   Int @default(0)

  matches1  MatchEntity[] @relation("player_matches1")
  matches2  MatchEntity[] @relation("player_matches2")

  @@map("player")
}
